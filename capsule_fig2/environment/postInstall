#!/usr/bin/env bash
set -euo pipefail

apt-get update
apt-get install -y --no-install-recommends python3
rm -rf /var/lib/apt/lists/*

python3 -c 'import csv, json, statistics; print("Fig. 2b audit runtime ready")'
